import tw from "twin.macro";
import styled from "styled-components";
const Title = styled.h1`
    ${tw` text-[3rem] text-1st-color leading-[3.75rem] font-semibold tracking-normal
    sm:(font-[2rem])
    `}
`

export default Title;