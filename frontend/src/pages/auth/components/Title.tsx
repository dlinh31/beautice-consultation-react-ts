import tw from 'twin.macro';
import styled from 'styled-components';

const Title = styled.h1`
    ${tw`text-1st-color leading-[3.75rem] my-0 font-semibold tracking-normal
    sm:(font-[2rem])
    `}
`

export default Title